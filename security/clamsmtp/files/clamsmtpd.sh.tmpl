#!/bin/sh
#
# $FreeBSD: ports/security/clamsmtp/files/clamsmtpd.sh.tmpl,v 1.1 2004/07/30 00:35:57 vanilla Exp $
#
# PROVIDE: clamsmtpd
# REQUIRE: DAEMON
# KEYWORD: FreeBSD
#
# Add the fellowing line to /etc/rc.conf to enable clamsmtpd:
#
# clamsmtpd_enable="YES"
#

. %%RC_SUBR%%

name=clamsmtpd
rcvar=`set_rcvar`

command=%%PREFIX%%/sbin/${name}
sig_stop=-KILL

load_rc_config ${name}

clamsmtpd_enable=${clamsmtpd_enable-"NO"}
clamsmtpd_conf=${clamsmtpd_conf-"%%PREFIX%%/etc/clamsmtpd.conf"}
clamsmtpd_pid=${clamsmtpd_pid-"%%CLAMAV_PID_DIR%%/clamsmtpd.pid"}
clamsmtpd_debug=${clamsmtpd_debug-"0"}

command_args="-f ${clamsmtpd_conf} -d ${clamsmtpd_debug} -p ${clamsmtpd_pid}"

pidfile=${clamsmtpd_pid}

run_rc_command "$1"
