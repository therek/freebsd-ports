#!/bin/sh
#
# $FreeBSD: ports/mail/postfix/scripts/configure,v 1.6 2002/01/30 19:59:40 dwcjr Exp $

for f in `find ${WRKSRC} -type f | xargs grep -l '\!\!PREFIX\!\!' ` ; do \
	mv $f $f.orig && sed s+!!PREFIX!!+$PREFIX+g < $f.orig > $f && \
		rm $f.orig
done
