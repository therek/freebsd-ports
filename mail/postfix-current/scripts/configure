#!/bin/sh
#
# $FreeBSD: ports/mail/postfix-current/scripts/configure,v 1.4 2000/09/13 20:50:45 billf Exp $

for f in `find ${WRKSRC} | xargs grep -l '\!\!PREFIX\!\!' ` ; do \
	mv $f $f.orig && sed s+!!PREFIX!!+$PREFIX+g < $f.orig > $f && \
		rm $f.orig
done
