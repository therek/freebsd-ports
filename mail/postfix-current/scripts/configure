#!/bin/sh
#
# $FreeBSD: ports/mail/postfix-current/scripts/configure,v 1.3 1999/08/30 12:06:27 peter Exp $

for f in `grep -r "\!\!PREFIX\!\!" ${WRKSRC} | cut -f 1 -d ':' | uniq` ; do \
	mv $f $f.orig && sed s+!!PREFIX!!+$PREFIX+g < $f.orig > $f && \
		rm $f.orig
done
