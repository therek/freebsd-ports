#!/bin/sh
# $FreeBSD: ports/irc/ircservices/files/ircservices.in,v 1.1 2006/06/26 13:28:49 mat Exp $
#
# PROVIDE: ircservices
# REQUIRE: LOGIN
# KEYWORDS: nojail shutdown

. %%RC_SUBR%%

name=ircservices
rcvar=${name}_enable

command="%%PREFIX%%/sbin/${name}"
command_args="-dir=%%PREFIX%%/etc/ircservices"
pidfile="/var/run/${name}.pid"
required_files="%%PREFIX%%/etc/ircservices/ircservices.conf \
		%%PREFIX%%/etc/ircservices/modules.conf"

load_rc_config ${name}

: ${ircservices_enable="NO"}

run_rc_command "$1"
